{
  "name": "egide",
  "version": "0.0.1",
  "private": true,
  "description": "Sovereign GRC platform — from boardroom signature to runtime enforcement",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@9.12.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "turbo": "^2.1.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.18.0",
    "pnpm": ">=9.12.0"
  }
}
