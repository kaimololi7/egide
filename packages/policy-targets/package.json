{
  "name": "@egide/policy-targets",
  "version": "0.0.1",
  "private": true,
  "description": "Policy compilation target adapters (Rego MVP per ADR 005). Scaffold; implementation lands at M5.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "biome lint . || true",
    "typecheck": "tsc --noEmit || true",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "typescript": "^6.0.3",
    "vitest": "^2.1.4"
  }
}
